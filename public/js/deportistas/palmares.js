/* public/js/modulos/palmares.js */

/**
 * Redirige al perfil "Mi ELO" del jugador seleccionado
 * @param {string} url - Ruta con el ID del jugador
 */
function irAPerfilPalmares(url) {
    // Efecto visual o lógica adicional si quisieras antes de redirigir
    window.location.href = url;
}